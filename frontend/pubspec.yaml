name: pos_urena
description: Sistema POS Móvil y Gestión de Inventario para Minimarket.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^1.10.0
  qr_code_scanner: ^1.0.1
  google_fonts: ^6.1.0
  provider: any
  intl: any
  fl_chart: any
  flutter_slidable: any
  url_launcher: ^6.3.2
  share_plus: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
